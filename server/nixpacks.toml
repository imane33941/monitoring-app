[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm i -g pnpm", "cd .. && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build:full"]

[start]
cmd = "node dist/main"
