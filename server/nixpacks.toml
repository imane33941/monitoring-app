[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@10 && /usr/local/lib/node_modules/pnpm/bin/pnpm.cjs install --frozen-lockfile"]

[phases.build]
cmds = ["/usr/local/lib/node_modules/pnpm/bin/pnpm.cjs run build:full"]

[start]
cmd = "node dist/main"
